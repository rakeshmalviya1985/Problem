/******************************************************************************

                              Online C++ Compiler.
               Code, Compile, Run and Debug C++ program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <iostream>
#include<bits/stdc++.h> 
using namespace std;

int getMin(int arr[], int size)
{
   
   if(size <=0 )
   return -1;
   
    for(int i=0;i<size;i++)
    cout<<arr[i];
    
    int min = INT_MAX;
    
    for(int i=0;i<size;i++)
    {
      if(min > arr[i])
       min = arr[i];
    }
       
     return min;
}

int sortFun(int arr[],int size)
{ 

    int ls = getMin(arr,size );
    
    
    
    
    int si =  -1;
    int i =  0;
    int j = 0;
    bool lst = false;
    while(i<size)
    {
        
        j= i+1;
        si = -1;
        cout<<"\nls = "<<ls;
        cout<<"\narr[i "<<i<<"]"<<arr[i];
        lst = false;
        while(j < size)
        {
            cout<<"\nwhile arr[j "<<j<<"]"<<arr[j] << " ls = "<<ls;
            if(arr[i] > arr[j] && arr[j] == ls)
            {
                si = j;
                break;

            }
            else if(arr[i] > arr[j] )
            {
                si = j;
            }
            else
            {
                si = -1;
                lst = true;
                cout<<"i in lstz";
            }
            
            
            j++;
        }
        
         cout<<"\narr[j "<<j<<"]"<<arr[j];
        int tmp =0;
        if( si != -1)
        {
            tmp = arr[i];
            arr[i] = arr[si];
            arr[si] =tmp;
            
        }
             if(lst == true)
             {   
               cout<<"\nAssinging lst as arr[i]";
               ls = arr[i];
             }
             else
               ls = arr[si];
                   
        cout<<"\n";
    for(int i=0;i<size;i++)
    cout<<arr[i];
        i++;
        j = 0;
    }
   
   cout<<"\n after sorting "; 
    for(int i=0;i<size;i++)
    cout<<arr[i];
}

int main()
{
 
 
 
    int arr[] = {1,0,2,1,0,2,2,1,0};

    int size = sizeof(arr)/sizeof(arr[0]);    
    sortFun(arr, size); 
    cout<<"\nHello World";
    

    return 0;
}
